# Copy to $HOME/netbox/env/postgres.env. Read by the netbox-postgres container.
# POSTGRES_DB and POSTGRES_USER are set in the quadlet; only the password here.
# Must match DB_PASSWORD in netbox.env.
POSTGRES_PASSWORD=changeme-db-password
